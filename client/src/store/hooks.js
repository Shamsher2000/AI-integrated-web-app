import { useDispatch, useSelector } from 'react-redux'

// Simple hooks - already optimal, no changes needed
export const useAppDispatch = () => useDispatch()
export const useAppSelector = useSelector

